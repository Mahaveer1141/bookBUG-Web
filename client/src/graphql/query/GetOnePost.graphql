query GetOnePost($postId: Float!) {
  getOnePost(postId: $postId) {
    id
    text
    imageUrl
    createdAt
    updatedAt
    creatorId
    creator {
      id
      username
      photoUrl
    }
    num_likes
    isLiked
  }
}
